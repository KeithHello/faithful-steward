import SwiftUI

extension Color {
    /// 7 大分類預設顏色
    static let categoryTithe = Color(hex: "#C47DA7")
    static let categoryFilial = Color(hex: "#D4A057")
    static let categorySocial = Color(hex: "#7DAEBF")
    static let categoryHousing = Color(hex: "#8C7DC4")
    static let categoryDebt = Color(hex: "#5B7FAD")
    static let categoryFoodTransport = Color(hex: "#5B8C5A")
    static let categoryFlexible = Color(hex: "#C47D6B")

    /// 語意色
    static let successGreen = Color(hex: "#16A34A")
    static let errorRed = Color(hex: "#DC2626")
    static let warningYellow = Color(hex: "#EAB308")
}
